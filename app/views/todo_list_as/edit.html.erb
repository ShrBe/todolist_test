<h1>id: <%= @todo_list.id %> のタスク編集</h1>

<%= form_for(@todo_list) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', todo_list_as_path %>
