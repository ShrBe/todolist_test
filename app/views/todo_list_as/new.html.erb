<h1>タスク新規作成ページ</h1>

<%= form_for(@todo_list) do |list| %>
  <%= list.label :content, 'メッセージ' %>
  <%= list.text_field :content %>
  
  <%= list.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', todo_list_as_path %>